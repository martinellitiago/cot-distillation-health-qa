# Analysis and tests. The released aggregate tables are sufficient to redraw
# the published figures, and the canonical seeds and code construct comparable
# seeded partitions (see README.md).
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
pytest>=7.4
pyarrow>=14.0          # parquet split files

# Canonical reconstructed GPU environment for rerunning the final analyses.
# The original environment was removed after the experiments; versions below
# are restricted to those preserved verbatim in run logs. See
# environment_h100_reconstructed.txt for provenance and the two Unsloth
# versions used across the reported runs.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.10.0+cu128
transformers==5.5.0
unsloth==2026.7.2
xformers==0.0.35
triton==3.6.0

# These dependencies were present, but their exact original versions were not
# recoverable. They remain deliberately unpinned rather than being guessed.
trl
datasets
peft
accelerate
bitsandbytes
tqdm>=4.66
