# Analysis and tests only -- these run on CPU and are enough to rebuild every
# table and figure in the paper from the released predictions.
numpy>=1.24
pandas>=2.0
scipy>=1.10
matplotlib>=3.7
pytest>=7.4
pyarrow>=14.0          # parquet split files

# Training and inference (GPU). The published runs used a single NVIDIA H100
# with the Unsloth/Transformers stack.
#
# TODO before the archived release: pin the EXACT versions from the run
# environment. Unsloth and TRL both moved fast during this project -- the code
# already carries a compatibility shim for the TRL rename of SFTConfig's
# max_seq_length -> max_length, and the loss-normalisation note in the README
# depends on the transformers version (the num_items_in_batch behaviour arrived
# in 4.46). Unpinned, these lines are a hint, not a reproduction.
#
# torch==
# transformers==
# trl==
# unsloth==
# datasets==
# peft==
# accelerate==
# bitsandbytes==
tqdm>=4.66
